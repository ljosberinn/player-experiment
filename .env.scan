# Read by `npm run dev:scan`, which reaches Vite as `--mode scan`. A bare
# `VITE_SCAN=true` in front of the script would not survive npm's cmd.exe on
# Windows; a mode file is how Vite sets one portably. See src/main.tsx.
VITE_SCAN=true
