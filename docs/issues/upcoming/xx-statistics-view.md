# xx — Statistics

The sidebar ships a dimmed, inert **Statistics** item, as the design draws it.

Decided in [plans/statistics.md](../../plans/statistics.md): two tabs, listening
and library, over a new `plays` log seeded by a last.fm history import. That plan
splits the work into phases 70, 75–78, 80 and 84, so this file is a pointer
rather than a piece of work.
