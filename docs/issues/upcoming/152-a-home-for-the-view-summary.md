# 152 — A home for the view summary

`statusbar-summary` ("n songs, x hours, size", from `viewSummary`) sits in the
status bar, far from the content it counts. Find it a better place.

Open: where. Candidates — a header line above the table/grid, the sidebar under
the current view, the player bar. Decide against the design source before
building.

If the status bar is left with only zoom and the update button, decide whether
it stays.

## Verification

- Every view (Songs, browse tabs, drill-in, playlist, search) shows its summary in the new place; Statistics none.
- Screenshots refreshed.
