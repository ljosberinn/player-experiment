# player

A local-only desktop music player for large mp3 libraries — iTunes 11 layout, modern styling.

Tauri v2 (Rust core) + React 19 / TypeScript / Vite.

## Prerequisites

- Node 24 (`node --version`)
- Rust stable (`rustup toolchain install stable`)
- Visual Studio Build Tools 2022 with the "Desktop development with C++" workload
- For e2e only: `cargo install tauri-driver --locked` and Microsoft Edge WebDriver
  (`msedgedriver.exe`, version-matched to the installed Edge). Point `MSEDGEDRIVER`
  at it if it is not in the default GitHub-runner location.

## Commands

| Command | What it does |
| --- | --- |
| `npm run tauri dev` | Run the app with HMR |
| `npm run typecheck` | `tsc --noEmit` for `src/` and `e2e/` |
| `npm run lint` / `lint:fix` | Biome check |
| `npm test` / `test:coverage` | Vitest unit + component tests |
| `npm run bindings` | Regenerate `src/ipc/bindings/` from the Rust types |
| `npm run e2e` | WebDriver smoke suite against the debug build |
| `cargo test --manifest-path src-tauri/Cargo.toml` | Rust unit + integration tests |

## Layout

```
src-tauri/src/   Rust core: commands/ (thin IPC seam), model.rs, error.rs
src/             React frontend; src/ipc/ is the only module that calls invoke
src/ipc/bindings Generated by ts-rs - never edit by hand
e2e/             WebdriverIO + tauri-driver smoke suite
```

Types cross the IPC boundary exactly once: they are declared in Rust with
`#[derive(TS)]` and exported to `src/ipc/bindings/`. CI fails if the checked-in
bindings drift from the Rust source.

## Contributing

`main` is protected. Work on `feat/*` branches and open a PR; CI (frontend,
rust, cargo-deny, e2e) must pass before merge.
